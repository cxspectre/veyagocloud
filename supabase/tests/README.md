# Database tests

SQL suites that run against the **live project** inside a transaction and
`ROLLBACK` at the end. Nothing they insert survives, and they read real policies
rather than a local copy — which is the point: RLS bugs are configuration bugs,
and a local re-creation can be green while production is not.

```bash
npm run check:db
```

The runner needs a Supabase **personal access token**. It reads
`SUPABASE_ACCESS_TOKEN`, and on macOS falls back to the one `supabase login`
already stored in the keychain. With neither it skips rather than fails, so CI
without a token stays green.

| Suite | What it proves |
|---|---|
| `01-roles.sql` | anon sees nothing; an employee sees the CRM but no finance, no money in the activity feed, and only the shared mailbox; `integration_secrets` is unreadable by every role that is not the service role |
| `02-write-guards.sql` | an assignee can move their task along but cannot re-file or rename it; soft delete is manager-only; a sent ticket reply cannot be edited; nobody can post as a colleague; resolution timestamps come from the database |
| `03-sync-path.sql` | every `ON CONFLICT` target the Edge Functions use actually exists, re-running a sync updates instead of duplicating, and the mail triggers maintain counts and match senders to the CRM |
| `04-mail-routing.sql` | a customer's reply finds its ticket through the `[#VYG-…]` in its subject, and mail that references no ticket does not open one |
| `05-project-team.sql` | a project's files are for its members, its owner and owners/admins only, and a member records and removes only their own uploads; nobody adds themselves, takes someone's project or picks a project's id; a project's contacts work at its company; the budget is for owners/admins, who can set, change and clear it; a project with files is archived, not deleted |
| `06-second-factor.sql` | for an account with a verified second factor, a session that skipped the code (`aal1`) has no role, reads and writes nothing and the overview refuses it — including through the policies that only ask who someone is (their own comments, their own mail attachments), which only the restrictive policy closes — while the same person at `aal2` is staff; a session with no role — one that owes its code, or an account with no employee row — is no manager, false rather than nothing, and cannot sweep notes as an owner would; no factor, or an unverified enrolment, owes nothing; the service role is unaffected; every RLS table in `public`, and `storage.objects`, carries the restrictive "second factor required" policy, and every view runs as the person asking |
| `07-overview-figures.sql` | the Overview's money stays in its own currency — the studio's declared currency shown, every other one listed, never added together, however a code was written; with none declared, the first active account's by name, then USD — for revenue, the revenue series and mix, and outstanding invoices, whose count and due date belong to the currency shown; last month is compared up to the same day, checked on the month ends that catch a wrong length, and `revenue_prev_through` names that day; income dated in UTC still counts today — in the tile, the chart and the mix, with last month then run to that day too — but nothing dated next month counts in this one and nothing dated after today counts at all; a one-month mix reads no day of last month, and its shares, of the categories it lists, add up to 100; revenue is money that reached the studio, each transaction counted by what it is — wires, a refund or a fee outside Stripe, anything entered by hand by its sign, and a card payment once, as its posted payout into the bank, or from Stripe's side when no synced account received one of that amount the other way, in that currency, from a day before to fourteen days after, a deposit still pending standing in for nothing — never Stripe's own charges, refunds or fees, a Stripe payout still pending, a transfer (one-sided or not), an adjustment, bank interest or a payment still pending, and expenses are spending less what came back; Stripe taking a balance back counts against revenue, and a bank row of a payout's amount the same way round is not its deposit; a synced transaction inserted or updated without a kind, by either sync, is read the way its sync meant it — Stripe known by its own names, never Pinstripe or The Stripe Agency, and a Stripe description a payout only when it starts with one — while a kind a sync wrote is kept and a row entered by hand or imported from a file gets none; anon cannot read what a transaction counts towards, and nobody writes through it; "today" is today in the viewer's time zone, and anything but an IANA name is UTC; callers that pass no arguments still get an answer; staff who are not managers get no money at all, and read no invoices, accounts or categories |
| `08-team-rules.sql` | an admin cannot make themselves owner, demote, deactivate, edit or delete the owner, take over a row with their own sign-in, add an owner, link a sign-in, or change a member's id or the address of someone who can sign in — while still adding members and changing everyone else's role and status; an owner can make and unmake owners but not change their own role or status; the service role never moves a member onto a different sign-in; nobody — the service role and the SQL editor included, one owner or every owner at once, by update or by delete — leaves the studio without an owner who can sign in; accepting an invitation still activates your own row; no address is on the team twice in different case; staff cannot select colleagues' phone numbers or notes, even with `select *`, but still read the directory and their own phone number through `employee_private()`, which gives owners and admins both; nothing lets anyone delete an employee |
| `09-connection-privacy.sql` | staff read the studio's mailbox and calendar connections and their own — not a colleague's personal mailbox, its address or its last error, directly or through `integration_status`; owners and admins read no more than that; a single policy decides it |
| `10-mail-leaves-inbox.sql` | a message archived or deleted in Outlook leaves the workspace inbox, and its conversation with it once nothing of it is left there — to Sent while it holds our reply, which stays in Sent; an archive sync that takes over the last inbox copy files the conversation too, and our next reply brings a filed conversation back under Sent; the same Graph id in another mailbox is untouched; nothing is deleted; a message moved back into the inbox takes over its stored copy rather than being stored twice, while a different message reusing a Message-ID is not merged into it; the daily comparison finds what a listing of Outlook's inbox no longer has, by Graph id or Message-ID, within the listing's reach and in that mailbox only; no message is stored without a folder; only the service role can file mail away or ask what to file |
| `11-ticket-routing.sql` | a customer's reply reopens a waiting ticket as well as a resolved one; a deleted ticket takes no replies and is not reopened, and "Create ticket" on its conversation opens a new ticket carrying the whole conversation; routing never opens a ticket by itself; a reply quoting a number too big for any ticket (`#VYG-99999999999`) or nothing but zeros (`#VYG-0`) goes through and reaches no ticket, while a ticket's number padded with zeros still reaches that ticket |
| `12-task-comments.sql` | only active staff read and add task comments, and never as a colleague; staff delete their own comments, owners and admins anyone's |
| `13-calendar-event-rules.sql` | only an event's creator, or an owner or admin, changes or deletes a hand-made event; nobody hands an event to a colleague, not even through `insert … on conflict`; synced events stay read-only |
| `14-crm-data-rules.sql` | a company's currency is three capital letters, and no code that only differed by case or spaces is left behind; a public mailbox's domain never becomes a company's domain, nor puts strangers into one company; a repeat enquirer at a public mailbox makes no company and keeps the link to the enquiry, and a contact with no company promoted twice gets exactly one |
| `15-project-task-rules.sql` | a project's team — its members and its owner — moves any task on the project along, as the task's assignee and owners/admins do; staff off the team, members of another project, deactivated members and sessions that skipped the code are refused, and staff still change nothing but a task's status |
| `16-invoice-links.sql` | an invoice is linked to the one live company whose name is its client's, whatever the case and spacing, or to its project's company; a project at another company is refused; staff read no invoices; invoices stored before 0051 are linked the same way |
| `17-project-activity.sql` | a task added, done, reopened or deleted, and a note or file added or removed, is logged under its project; a file's name shows only to people who can see the file; activity stored before 0052 is filed under its project too; staff cannot write activity directly |
| `18-crm-functions.sql` | a contact and their company are made in one call; merging two companies or two contacts moves every reference to the one kept, and a merge that would collide or loop is refused; a company is given a client number when it first becomes a client, and the app never writes one. The suite uses about five client numbers on the project it runs against and hands them back, and its last section briefly locks `crm_companies` and `finance_invoices` while it swaps `crm_merge_references()`, until the rollback |
| `19-ticket-merge.sql` | merging one ticket into another moves its messages, notes and conversations and adds a note to both; the refusals hold; a reply that quotes the merged ticket's number, or comes in its conversation, reaches the ticket kept |
| `21-ticket-workflow.sql` | opening a ticket from a conversation keeps the sender's raw name and address even when the CRM has no contact for them; a reply sent from Mail or Outlook, not through the ticket's own reply box, is filed on the ticket its conversation is attached to, with no author, and is not filed again if the sync sees it twice or if send-ticket-reply already linked it itself; a conversation with no live ticket stays mail; response targets are exactly the four priorities support_tickets.priority checks against, staff read them and only a manager changes the minutes; a new ticket's due-by times come from its priority and move when the priority does; a ticket attachment is recorded only once it is actually uploaded, with the size and type storage actually has; whoever uploaded a file or a manager removes it, nobody else, and anon reaches none of it; email_log accepts its two new kinds |
| `27-mail-unread-counts.sql` | `mail_unread_counts()` answers only what `mail_threads`' own policy already lets this session read — the studio's mailbox and this person's own, never a colleague's personal one — counting only unread threads still in the inbox, the same thing the reading pane already means by unread; it stays `security invoker` rather than a rule of its own; anon is refused outright, and someone signed in who is not on the team counts nothing |
| `29-agenda-event-search.sql` | `search_events` finds a past meeting or a non-project event by a word in its title, detail or location — never a cancelled one, and never one on a colleague's own personal calendar, only the studio's or the searcher's own (0026's rule, applied automatically: the function is security_invoker); `p_limit` is honoured up to a hard cap of 50 however high a caller asks; a blank or null query asks nothing |

`06` to `19` test migrations 0040 to 0054, `21` tests 0056, and `29` tests
migration 0064: until those are live they fail, so dry-run them first — each
suite with the migrations applied inside its own transaction, and rolled
back with it.

Each suite prints one row per check with `PASS` / `FAIL`. The runner exits
non-zero if any row says FAIL.

## Adding a check

Insert into the `results` temp table — `(name, expected, actual, pass)` — and
keep the suite inside its `begin` / `rollback`. A check that needs a
non-manager runs as the inactive assistant, woken for the length of the
transaction only.

Sessions carry `"aal":"aal2"` in their claims: since 0040 a session that has
not entered its second factor is refused everything when the account has one,
so a check about what staff may do runs as staff who entered their code. Only
`06-second-factor.sql` uses `aal1`, on purpose.

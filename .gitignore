# OS / editor cruft
.DS_Store
**/.DS_Store

# Local Claude Code tooling (not part of the site)
.claude/

# Build tooling (devDependencies — never shipped to the browser)
node_modules/

# Local Supabase credentials for the build (anon key + project URL)
.env
